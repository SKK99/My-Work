/* @file array.h
 * @brief Prototypes for the functions called in main.c
 * @author Shankar Karn
 * @date 21-02-2022
 */
#define MAX_LEN 20
char *swap_elements(char arr[][MAX_LEN], int index);
int isVowel(char ch);
void display(char arr[][MAX_LEN]);
char* mystrcat(char *s1, char *s2);
char* remove_vowels(char arr[][MAX_LEN], int index);
